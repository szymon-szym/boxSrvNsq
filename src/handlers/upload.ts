// const BoxSDK = require('box-node-sdk')

// // create model for input

// class BoxActionMessage {
//     targetFolder:string

// }

// await this.client.files
// .uploadFile(body.targetFolder, `${body.fileToUploadName} ${Date.now()}${Math.random().toString()}.${body.fileToUploadType}`, Buffer.from(body.fileToUpload.data))